# v1.20.5

## Refactor — overlap consolidation

### LSP: 2 `LspManager`, 3 tables, 2 installers
- **Single registry:** `src/lsp/registry.ts` với `LSP_REGISTRY` 12 entries (extensions, serverBinary, args, install/verify, alternatives) + `getLanguageFromPath`/`detectLanguage`/`resolveRegistryEntry`.
- **Single path:** `src/utils/lsp-path.ts` export duy nhất `getLspInstallDir()` + helpers `getLspNodeBinDir()`/`getLspLegacyBinDir()` (thay 6 inline `path.join`).
- **Rename clash:** `tools/lsp-manager.ts` `LspManager` → `LspStatusManager` (singleton `lspStatusManager`), `lsp/manager.ts` giữ client pool `LspManager`.
- **Installers chung data:** `ensureLspInstalled` (on-demand) và `ensureLspServers` (startup batch, giữ shape test 4/8/9 calls) đều đọc registry chung, `STARTUP_LANGUAGES` mang `startupFallbackCommand` cho Python.

### Memory: 4 abstraction overlap
- **Single filter config:** `src/services/memory-config.ts` export `setMemoryFilterConfig`/`getMemoryFilterConfig`, `tools/memory.ts` và `services/vector-memory.ts` import chung (xóa duplicate, bỏ `as any`).
- **Block ownership API:** `readProjectMemoryBody()`/`writeProjectMemoryBody()` dùng `readMemoryBlock`/`buildFrontmatter` (giữ frontmatter, filter, limit oldest-first, check read-only).
- **`autoSaveProjectMemory` (`src/index.ts`):** không còn `fs.writeFileSync` trực tiếp, delegate qua block API → được filter/limit/read-only.
- **Journal:** `hive_journal_write` giờ qua `filterSensitiveData`, doc ghi rõ journal (audit) vs typed (scoped knowledge) vs block (prompt injection) vs vector (semantic).

### Installer skip-logic duplicate
- `tool-installer.ts`/`lsp-autoinstall.ts`/`rtk-installer.ts` đều `HIVE_DISABLE_AUTO_INSTALL` + `hive-e2e` HOME check. Nay `src/utils/skip-install.ts` export `shouldSkipAutoInstall()` duy nhất, 3 nơi import.

### Verify
- `bun run build` OK (141 modules, 1.11 MB), `bun test` 595 pass / 0 fail / 1 skip (hive-core 148 pass).
