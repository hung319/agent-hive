# v1.20.4

## Chore

### Dead code prune
- Xóa 9 files zero importer: `hooks/hive-hooks.ts`, `utils/{pattern-learner,background-sync,auto-summary,lazy-loader,model-selector}`, `hive-core/services/{memoryService,askService}`, `hive-core/templates.ts`. Không còn import nào (đã kiểm tra `grep -r` + build 586 pass).

### plugin.json stale
- `version` 1.5.5 → 1.20.4, `tools` bổ sung đầy đủ ~60 tools (thiếu `auto_cr_*`, `ast_grep_*`, `code_search*`, `call_graph*`, `pty_*`, `gitingest`…), `agents` thu gọn 13 → 6 (`zetta`, `scout-researcher`, `forager-worker`, `hygienic-reviewer`, `codebase-locator`, `codebase-analyzer`), `hooks` rỗng (hooks cũ `hive.todo-enforcer` đã xóa cùng `hive-hooks.ts`).

### agentMode unified/dedicated
- Trước byte-identical (dedicated là no-op). Nay dedicated log `warn` “deprecated — using unified set” và vẫn dùng unified set, tránh silent no-op.

### CHANGELOG
- Trước frozen 1.3.1. Nay thêm stub cho 1.20.0-1.20.3 + note “see docs/releases/v*.md from 1.4.0 onward”.

### Giữ nguyên (theo yêu cầu “ko”)
- LSP `LspManager` duplicate, 3 bảng lang→server, 2 installer; Memory 4 abstraction; Installer skip-logic duplicate — chưa gộp, sẽ làm riêng khi bạn confirm.
