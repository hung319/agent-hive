{
  "mcpServers": {
    "websearch": {
      "command": "npx",
      "args": ["-y", "exa-mcp-server"],
      "env": {
        "EXA_API_KEY": "${EXA_API_KEY}"
      },
      "description": "Exa AI web search (requires API key)"
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "description": "Library documentation lookup"
    },
    "grep_app": {
      "command": "npx",
      "args": ["-y", "grep-mcp"],
      "description": "GitHub code search via grep.app"
    },
    "codegraph": {
      "command": "codegraph",
      "args": ["serve", "--mcp"],
      "description": "Pre-indexed code knowledge graph. The Hive plugin auto-installs it to ~/.config/opencode/hive; for manual setups install via: curl -fsSL https://raw.githubusercontent.com/colbymchenry/codegraph/main/install.sh | sh"
    }
  }
}
